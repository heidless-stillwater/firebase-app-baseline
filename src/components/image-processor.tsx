'use client';

import { Card, CardContent, CardHeader, CardTitle } from '@/components/ui/card';

export function ImageProcessor() {
  return (
    <Card>
      <CardHeader>
        <CardTitle>Image Processor</CardTitle>
      </CardHeader>
      <CardContent>
        <p>This is where the image processing functionality will go.</p>
      </CardContent>
    </Card>
  );
}
